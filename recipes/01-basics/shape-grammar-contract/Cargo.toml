[package]
name = "sim-shape-recipe-shape-grammar-contract"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Runnable Shape grammar contract cookbook recipe."
readme = "README.md"
publish = false

[dependencies]
sim-codec = { version = "0.3.0" }
sim-codec-json = { version = "0.4.0" }
sim-codec-lisp = { version = "0.4.0" }
sim-kernel = "0.4.0"
sim-shape = { path = "../../..", version = "0.4.0" }
sim-test-support = { version = "0.1.4" }

[lints]
workspace = true
